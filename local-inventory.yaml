all:
  children:
    local:
      children:
        edge_management:
          hosts:
            edge-manager-local:
              
  vars:
    ansible_host: 10.1.1.36
    ansible_user: ansible-user
    ansible_password: ansiblePassword
    ansible_become_password: ansiblePassword
    external_connection: ens160 
    internal_connection: ens192
    lab_wifi_network:
      ssid: RedHatEdge
      password: 'AnythingHere'
